package codelab01;

public class Main {

    // Create a method that prints the string "Hello World!"
    public static void printHelloWorld() {
    }

    //Create a method (printAny) that prints any string you give it.
    // CREATE HERE

    // Using method overloading, create two methods with the same name (printNumber).
    // Both have one parameter.
    // Both should print a message, although the message differs based on the type (Integer or Double, not int or double):
    //      I'm an int: <parameter>
    //      I'm a double: <parameter>
    // CREATE HERE

    // Create a method that returns the number it receives plus 1
    public static int addByOne(int i) {
        return 0;
    }

}
