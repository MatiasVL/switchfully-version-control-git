package com.switchfully.vaadin.exercise.exercise_01_basic_layouts;

import com.vaadin.flow.component.orderedlayout.VerticalLayout;
import com.vaadin.flow.router.Route;

@Route("exercises/1")
public class HomePage extends VerticalLayout {

    public HomePage() {
        // TODO 01-basic_layouts: Using VerticalLayout and HorizontalLayout, create a button layout resembling the buttons of an old school cellphone.

        // Use the Button component to create the buttons:
        // Button one = new Button("1");
    }

}